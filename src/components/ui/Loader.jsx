import './Loader.css'; // Crea este archivo CSS para definir el estilo del spinner

const Loader = () => {
  return (
    <div className="loader">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;